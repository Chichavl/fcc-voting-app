import React, { Component } from 'react';

export default class NoMatch extends Component {
  render() 
  {
    const NoMatch = (
            <h1>
              404 - NoMatch
            </h1>
    );
     return NoMatch;
  }
}